import React from 'react';

import { Header, Subheading } from '../styled';
import Register from '../components/Register';

export default () => (
  <div>
    <Header>Cloud.</Header>
    <Header>Now for the rest of us.</Header>
    <Subheading>Dripbox lets you store your files in the cloud for free</Subheading>
    <Register />
  </div>
);
